{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Middleware FHIR",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "main:app",
                "--reload",
                "--host",
                "0.0.0.0",
                "--port",
                "8000"
            ],
            "jinja": true,
            "cwd": "${workspaceFolder}/middleware",
            "envFile": "${workspaceFolder}/middleware/.env"
        }
    ]
}